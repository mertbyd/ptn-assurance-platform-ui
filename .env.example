# Auth host (Authenticator — login, logout, tenant, organization)
NEXT_PUBLIC_AUTH_ORIGIN=https://localhost:44323

# Test Module composition host (checkers, scenarios, runs, notifications)
NEXT_PUBLIC_TEST_MODULE_ORIGIN=https://localhost:44366

# Agent gateway (authoring conversation and SSE)
NEXT_PUBLIC_AGENT_ORIGIN=http://localhost:4310
